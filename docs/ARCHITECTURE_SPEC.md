# Architecture Specification

## 시스템 개요

```
┌─────────────────────────────────────────────────────────────────┐
│                        Client (Browser)                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Next.js Frontend                      │   │
│  │  • React 19 Components                                   │   │
│  │  • Zustand State Management                              │   │
│  │  • Tailwind CSS Styling                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Next.js Server (Vercel)                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Server Components                     │   │
│  │  • Static Page Generation (SSG)                          │   │
│  │  • Metadata / SEO                                        │   │
│  │  • API Routes (필요시)                                    │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     External Services                           │
│  • Geolocation API (lunch-roulette)                            │
│  • LocalStorage / IndexedDB                                     │
│  • Future: Supabase (실시간 데이터)                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## Server-Side 역할

### 1. Static Generation (SSG)

Next.js 서버에서 **빌드 타임**에 처리

| 역할 | 설명 | 적용 대상 |
|------|------|----------|
| **HTML 생성** | 초기 HTML 셸 생성 | 모든 앱 |
| **메타데이터** | title, description, OG tags | 모든 앱 |
| **레이아웃** | 공통 헤더/푸터 렌더링 | 갤러리, 공통 |
| **정적 자산** | JS/CSS 번들링 및 최적화 | 모든 앱 |

```tsx
// app/salary-calculator/page.tsx
import { Metadata } from 'next';

// ✅ Server-side: 빌드 타임에 생성
export const metadata: Metadata = {
  title: '급여 실수령액 계산기 | SeolCoding Apps',
  description: '연봉, 월급 기준 4대보험, 세금 공제 후 실수령액 계산',
  openGraph: {
    title: '급여 실수령액 계산기',
    description: '연봉 협상 전 필수! 실수령액을 미리 계산해보세요',
    images: ['/og/salary-calculator.png'],
  },
};

// ✅ Server Component (기본)
export default function SalaryCalculatorPage() {
  return (
    <main>
      {/* Client Component를 import */}
      <SalaryCalculatorApp />
    </main>
  );
}
```

### 2. API Routes (선택적)

필요시 `/app/api/` 에서 서버 로직 처리

| API | 용도 | 우선순위 |
|-----|------|----------|
| `/api/share` | 결과 공유 링크 생성 | 낮음 |
| `/api/og-image` | 동적 OG 이미지 생성 | 중간 |
| `/api/analytics` | 사용 통계 수집 | 낮음 |

```tsx
// app/api/og-image/route.ts (예시)
import { ImageResponse } from 'next/og';

export async function GET(request: Request) {
  const { searchParams } = new URL(request.url);
  const title = searchParams.get('title');

  return new ImageResponse(
    <div style={{ /* ... */ }}>
      {title}
    </div>,
    { width: 1200, height: 630 }
  );
}
```

### 3. 서버 컴포넌트 활용

| 컴포넌트 | 서버/클라이언트 | 이유 |
|----------|----------------|------|
| `layout.tsx` | Server | 정적 레이아웃 |
| `page.tsx` (wrapper) | Server | 메타데이터 |
| `GalleryGrid` | Server | 정적 앱 목록 |
| `AppCard` | Server | 정적 카드 |
| `Footer` | Server | 정적 콘텐츠 |

---

## Client-Side 역할

### 1. 인터랙티브 앱 로직

모든 미니앱의 **핵심 로직**은 클라이언트에서 실행

```tsx
// app/salary-calculator/components/SalaryCalculatorApp.tsx
'use client'; // ← 필수

import { useState } from 'react';
import { useSalaryStore } from '../store/salaryStore';

export function SalaryCalculatorApp() {
  // ✅ Client-side: 브라우저에서 실행
  const { salary, setSalary, calculate } = useSalaryStore();

  return (
    <div>
      <input
        value={salary}
        onChange={(e) => setSalary(e.target.value)}
      />
      <button onClick={calculate}>계산하기</button>
    </div>
  );
}
```

### 2. 앱별 클라이언트 책임

| 앱 | 클라이언트 책임 |
|----|----------------|
| **salary-calculator** | 입력 폼, 계산 로직, 차트 렌더링 |
| **chosung-quiz** | 게임 상태, 타이머, 정답 체크 |
| **ladder-game** | Canvas 렌더링, 애니메이션, 사다리 로직 |
| **balance-game** | 선택 UI, 결과 통계 |
| **bingo-game** | 그리드 상태, 빙고 체크, 호스트/플레이어 분기 |
| **dutch-pay** | 참가자 관리, 금액 분배 계산 |
| **gpa-calculator** | 과목 입력, 학점 계산 |
| **ideal-worldcup** | 토너먼트 로직, 이미지 표시 |
| **live-voting** | 투표 상태, 실시간 차트 |
| **random-picker** | 항목 관리, 랜덤 선택 애니메이션 |
| **group-order** | 메뉴 선택, QR 생성, 실시간 동기화 |
| **id-validator** | 입력 검증, 체크섬 계산 |
| **lunch-roulette** | 위치 API, 룰렛 애니메이션 |
| **rent-calculator** | 전월세 계산, 대출 시뮬레이션 |
| **student-network** | 프로필 입력, 태그 선택, 네트워크 뷰 |
| **team-divider** | 참가자 입력, 팀 배정 알고리즘 |

### 3. 상태관리 (Zustand)

```tsx
// app/salary-calculator/store/salaryStore.ts
'use client';

import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface SalaryState {
  annualSalary: number;
  dependents: number;
  result: CalculationResult | null;
  setAnnualSalary: (value: number) => void;
  calculate: () => void;
}

export const useSalaryStore = create<SalaryState>()(
  persist(
    (set, get) => ({
      annualSalary: 50000000,
      dependents: 1,
      result: null,
      setAnnualSalary: (value) => set({ annualSalary: value }),
      calculate: () => {
        const { annualSalary, dependents } = get();
        // 계산 로직...
        set({ result: calculatedResult });
      },
    }),
    { name: 'salary-calculator-storage' }
  )
);
```

### 4. 브라우저 API 사용

| API | 사용 앱 | 용도 |
|-----|---------|------|
| **LocalStorage** | 전체 | 상태 영속화 (Zustand persist) |
| **Geolocation** | lunch-roulette | 현재 위치 |
| **Clipboard** | 결과 공유 | 복사 기능 |
| **Canvas** | ladder-game | 사다리 렌더링 |
| **Web Share** | 전체 (선택) | 모바일 공유 |

```tsx
// lunch-roulette: Geolocation 사용 예시
'use client';

export function LocationPicker() {
  const [location, setLocation] = useState<GeolocationPosition | null>(null);

  useEffect(() => {
    navigator.geolocation.getCurrentPosition(
      (pos) => setLocation(pos),
      (err) => console.error(err)
    );
  }, []);

  return <div>위치: {location?.coords.latitude}</div>;
}
```

---

## 렌더링 전략 요약

### 페이지별 렌더링 방식

```
                    ┌─────────────────────────────────────┐
                    │           Next.js App               │
                    └─────────────────────────────────────┘
                                      │
        ┌─────────────────────────────┼─────────────────────────────┐
        │                             │                             │
        ▼                             ▼                             ▼
┌───────────────┐           ┌───────────────┐           ┌───────────────┐
│   SSG Pages   │           │  SSG + CSR    │           │   API Routes  │
│   (Static)    │           │   (Hybrid)    │           │   (Dynamic)   │
├───────────────┤           ├───────────────┤           ├───────────────┤
│ • /           │           │ • /salary-*   │           │ • /api/og     │
│   (Gallery)   │           │ • /chosung-*  │           │ • /api/share  │
│ • /about      │           │ • /ladder-*   │           │               │
│ • /privacy    │           │ • ... (16개)  │           │               │
└───────────────┘           └───────────────┘           └───────────────┘
        │                             │                             │
        ▼                             ▼                             ▼
   빌드 타임에              빌드: HTML 셸            요청 시 실행
   HTML 생성                런타임: React Hydration   서버에서 처리
```

### 컴포넌트 분류

```tsx
// ✅ Server Component (기본값)
// - 메타데이터 설정
// - 정적 UI 렌더링
// - 데이터 fetching (필요시)
export default function Page() {
  return <ClientApp />;
}

// ✅ Client Component ('use client')
// - 상태관리 (useState, Zustand)
// - 이벤트 핸들러 (onClick, onChange)
// - 브라우저 API (localStorage, geolocation)
// - 훅 사용 (useEffect, useRef)
'use client';
export function ClientApp() {
  const [state, setState] = useState();
  return <button onClick={() => setState(...)}>Click</button>;
}
```

---

## 디렉토리 구조 상세

```
src/
├── app/
│   ├── layout.tsx                 # 🔵 Server: 루트 레이아웃
│   ├── page.tsx                   # 🔵 Server: 갤러리 페이지
│   ├── globals.css                # 전역 스타일
│   │
│   ├── salary-calculator/
│   │   ├── page.tsx               # 🔵 Server: 메타데이터 + wrapper
│   │   ├── components/
│   │   │   ├── SalaryApp.tsx      # 🟢 Client: 메인 앱
│   │   │   ├── InputForm.tsx      # 🟢 Client: 입력 폼
│   │   │   ├── ResultCard.tsx     # 🟢 Client: 결과 카드
│   │   │   └── DeductionChart.tsx # 🟢 Client: 차트
│   │   ├── store/
│   │   │   └── salaryStore.ts     # 🟢 Client: Zustand store
│   │   ├── utils/
│   │   │   └── calculations.ts    # 공유 (순수 함수)
│   │   └── types/
│   │       └── index.ts           # 공유 (타입)
│   │
│   ├── chosung-quiz/
│   │   └── ... (동일 구조)
│   │
│   └── api/                       # 🔵 Server: API Routes
│       └── og-image/
│           └── route.ts
│
├── components/
│   ├── ui/                        # 🟢 Client: Radix UI 컴포넌트
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   ├── input.tsx
│   │   └── ...
│   └── shared/                    # 혼합
│       ├── AppCard.tsx            # 🔵 Server: 정적 카드
│       └── ThemeToggle.tsx        # 🟢 Client: 테마 토글
│
├── lib/
│   ├── utils.ts                   # 공유: cn() 등 유틸
│   └── constants.ts               # 공유: 상수
│
└── styles/
    └── themes.css                 # 테마 변수
```

**범례**
- 🔵 Server Component
- 🟢 Client Component ('use client')

---

## 데이터 흐름

### 현재 (Vite): 100% 클라이언트

```
Browser
   │
   ▼
┌─────────────────────────────┐
│      React App (CSR)        │
│  • 상태관리                  │
│  • 계산 로직                 │
│  • UI 렌더링                 │
│  • LocalStorage             │
└─────────────────────────────┘
```

### 마이그레이션 후 (Next.js): 하이브리드

```
Browser                              Server (Vercel)
   │                                      │
   │  1. 페이지 요청                       │
   │ ─────────────────────────────────▶   │
   │                                      │
   │  2. 정적 HTML + JS 번들              │
   │ ◀─────────────────────────────────   │
   │                                      │
   ▼
┌─────────────────────────────┐
│     React Hydration         │
│  • Client Component 활성화   │
│  • 이벤트 바인딩             │
└─────────────────────────────┘
   │
   ▼
┌─────────────────────────────┐
│    Interactive App (CSR)    │
│  • 상태관리 (Zustand)        │
│  • 계산 로직                 │
│  • UI 업데이트               │
│  • LocalStorage             │
└─────────────────────────────┘
```

---

## 미래 확장: Supabase 통합

### Phase 2에서 추가 가능한 서버 기능

| 기능 | 서버 역할 | 클라이언트 역할 |
|------|----------|----------------|
| **결과 저장** | Supabase에 저장 | UI, 저장 버튼 |
| **결과 공유** | 고유 URL 생성 | 공유 버튼, 복사 |
| **실시간 동기화** | Supabase Realtime | 상태 구독 |
| **사용자 인증** | Supabase Auth | 로그인 UI |

```tsx
// 미래: 서버 액션 예시
// app/salary-calculator/actions.ts
'use server';

import { createClient } from '@/lib/supabase/server';

export async function saveResult(result: CalculationResult) {
  const supabase = createClient();

  const { data, error } = await supabase
    .from('salary_results')
    .insert(result)
    .select('id')
    .single();

  return { shareUrl: `/share/${data.id}` };
}
```

---

## 요약: 역할 분담

| 영역 | Server | Client |
|------|--------|--------|
| **HTML 생성** | ✅ SSG | - |
| **SEO/메타데이터** | ✅ | - |
| **정적 레이아웃** | ✅ | - |
| **JS 번들 최적화** | ✅ | - |
| **상태관리** | - | ✅ Zustand |
| **계산 로직** | - | ✅ |
| **UI 인터랙션** | - | ✅ |
| **브라우저 API** | - | ✅ |
| **차트 렌더링** | - | ✅ Recharts |
| **Canvas** | - | ✅ |
| **API Routes** | ✅ (선택) | - |
| **데이터베이스** | ✅ (미래) | - |
