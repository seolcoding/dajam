# PRD: Audience Engage (슬라이드 인터랙션)

> **Slido/Mentimeter 대안** - 슬라이드 기반 실시간 청중 참여 통합 플랫폼

## 1. 개요

### 1.1 목적
발표자가 PPT/PDF/Google Slides를 업로드하고, 청중이 실시간으로 다양한 인터랙션에 참여하는 **통합 인터랙티브 프레젠테이션 플랫폼**.

**6개 리얼타임 앱을 Scene으로 통합**하여 하나의 플랫폼에서 모든 인터랙션 제공.

### 1.2 핵심 가치
- **즉시 참여**: 6자리 코드로 회원가입 없이 참여
- **통합 인터랙션**: 퀴즈, 투표, 워드클라우드, 빙고 등 모든 인터랙션을 하나로
- **슬라이드 통합**: 발표 자료 사이에 인터랙션 삽입
- **하이브리드 접근**: 독립 앱으로도, audience-engage 내에서도 사용 가능

### 1.3 참고 분석
- **Claper**: `claudedocs/claper-analysis.md`
- **Auden**: `claudedocs/auden-architecture-analysis.md`

---

## 2. 아키텍처 결정

### 2.1 통합 방식: 하이브리드

| 결정 | 선택 |
|------|------|
| 통합 방식 | **하이브리드** - audience-engage 내에서도, 독립 앱으로도 사용 가능 |
| 라우트 처리 | **유지 (별칭)** - /realtime-quiz도 유지, /audience-engage?scene=quiz도 지원 |
| 코드 공유 | **공유 컴포넌트** - `@/features/interactions/`에서 코드 공유 |

### 2.2 통합 대상 앱 (Scene 타입)

| Scene | 기존 라우트 | 통합 후 접근 | 설명 |
|-------|------------|--------------|------|
| `quiz` | `/realtime-quiz` | `/audience-engage?scene=quiz` | 실시간 퀴즈 |
| `vote` | `/live-voting` | `/audience-engage?scene=vote` | 실시간 투표 |
| `this-or-that` | `/this-or-that` | `/audience-engage?scene=this-or-that` | 이것저것 |
| `word-cloud` | `/word-cloud` | `/audience-engage?scene=word-cloud` | 워드 클라우드 |
| `personality` | `/personality-test` | `/audience-engage?scene=personality` | 성격 테스트 |
| `bingo` | `/human-bingo` | `/audience-engage?scene=bingo` | 휴먼 빙고 |
| `slides` | - | `/audience-engage?scene=slides` | 슬라이드 발표 (기본) |

### 2.3 하이브리드 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│                    audience-engage (통합 플랫폼)                 │
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    Scene Manager                           │  │
│  │  ┌───────┬───────┬─────────┬───────┬───────┬───────┐      │  │
│  │  │slides │ quiz  │this-or- │ word  │person-│ bingo │      │  │
│  │  │       │       │ that    │ cloud │ ality │       │      │  │
│  │  └───┬───┴───┬───┴────┬────┴───┬───┴───┬───┴───┬───┘      │  │
│  └──────┼───────┼────────┼────────┼───────┼───────┼──────────┘  │
│         │       │        │        │       │       │             │
│         ▼       ▼        ▼        ▼       ▼       ▼             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │           공유 컴포넌트 (@/features/interactions)          │  │
│  │                                                            │  │
│  │  • QuizGame        • VotePoll       • ThisOrThatGame      │  │
│  │  • WordCloudInput  • PersonalityQuiz • BingoBoard         │  │
│  │  • SlideViewer     • ReactionBar     • QAPanel            │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              ▲                                   │
└──────────────────────────────┼───────────────────────────────────┘
                               │ (import)
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│ /realtime-quiz│    │ /this-or-that │    │ /word-cloud   │
│ (독립 라우트) │    │ (독립 라우트) │    │ (독립 라우트) │
│               │    │               │    │               │
│ 동일 컴포넌트 │    │ 동일 컴포넌트 │    │ 동일 컴포넌트 │
│ 사용          │    │ 사용          │    │ 사용          │
└───────────────┘    └───────────────┘    └───────────────┘
```

### 2.4 사용자 선택 요약

| 항목 | 선택 |
|------|------|
| 앱 이름 | `audience-engage` |
| 슬라이드 소스 | PPT/PDF + Google Slides (Claper 방식) |
| 인터랙션 | 투표, Q&A, 채팅, 이모지 + **6개 앱 통합** |
| 뷰 구조 | Host + Participant (2뷰) |

---

## 3. 기능 명세

### 3.1 슬라이드 관리

#### A. 슬라이드 소스 지원

| 소스 | 구현 방식 | 우선순위 |
|------|-----------|----------|
| **Google Slides** | iframe 임베드 (`/embed?start=false`) | P0 |
| **PDF 업로드** | pdf.js로 클라이언트 변환 → JPG | P1 |
| **PPT 업로드** | Cloudinary API로 변환 | P2 |
| **이미지 직접 업로드** | Supabase Storage | P1 |

#### B. 슬라이드 처리 플로우

```
┌─────────────────────────────────────────────────────────────┐
│                    슬라이드 업로드                           │
├─────────────────────────────────────────────────────────────┤
│  Google Slides URL 입력                                      │
│    └── iframe src 저장                                       │
├─────────────────────────────────────────────────────────────┤
│  PDF 업로드                                                  │
│    └── pdf.js로 각 페이지 → canvas → JPG (브라우저)          │
│    └── Supabase Storage에 업로드                             │
│    └── slide_images 테이블에 URL 저장                        │
├─────────────────────────────────────────────────────────────┤
│  이미지 업로드                                               │
│    └── Supabase Storage에 직접 업로드                        │
│    └── slide_images 테이블에 URL 저장                        │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 인터랙션 기능

#### A. 실시간 투표/폴링 (live-voting 재활용)

```typescript
interface Poll {
  id: string;
  sessionId: string;
  slidePosition: number;  // 특정 슬라이드에 연결
  title: string;
  type: 'single' | 'multiple';
  options: PollOption[];
  showResults: boolean;
  enabled: boolean;
  createdAt: string;
}
```

- 호스트가 슬라이드별 투표 생성
- 참여자가 실시간 투표
- 결과 실시간 표시 (차트)

#### B. Q&A (질문 수집)

```typescript
interface Question {
  id: string;
  sessionId: string;
  body: string;
  authorName: string;
  authorId: string | null;  // 익명 가능
  likeCount: number;
  isHighlighted: boolean;  // 호스트 하이라이트
  isAnswered: boolean;
  createdAt: string;
}
```

- 참여자가 질문 제출
- 다른 참여자가 좋아요
- 호스트가 하이라이트/답변 완료 표시
- 좋아요 순 정렬

#### C. 실시간 채팅

```typescript
interface ChatMessage {
  id: string;
  sessionId: string;
  slidePosition: number;  // 슬라이드별 채팅
  authorName: string;
  authorId: string | null;
  body: string;
  createdAt: string;
}
```

- 슬라이드별 채팅 스레드
- 익명/실명 선택
- 호스트 삭제 권한

#### D. 이모지 리액션

```typescript
interface EmojiReaction {
  type: '👍' | '❤️' | '😂' | '👏' | '🎉';
  count: number;
}
```

- 화면에 떠오르는 이모지 애니메이션
- 실시간 카운트 집계
- Framer Motion으로 구현

### 3.3 다른 앱 연동

호스트가 슬라이드 사이에 다른 앱 삽입 가능:

| 앱 | 연동 방식 |
|-----|-----------|
| `realtime-quiz` | 퀴즈 세션 임베드 |
| `this-or-that` | 이것저것 선택 임베드 |
| `word-cloud` | 워드클라우드 임베드 |
| `live-voting` | 투표 세션 임베드 |

```typescript
interface SlideItem {
  id: string;
  type: 'slide' | 'poll' | 'quiz' | 'this-or-that' | 'word-cloud' | 'embed';
  position: number;

  // type별 데이터
  slideUrl?: string;        // type: 'slide'
  pollId?: string;          // type: 'poll'
  embedUrl?: string;        // type: 'embed' (YouTube, 외부)
  linkedSessionCode?: string; // type: 'quiz' | 'this-or-that' | 'word-cloud'
}
```

---

## 4. 아키텍처

### 4.1 폴더 구조 (하이브리드 아키텍처)

#### A. 공유 컴포넌트 (`@/features/interactions/`)

```
src/features/interactions/
├── index.ts                      # 공통 export
├── types.ts                      # 공통 타입
├── hooks/
│   ├── useInteractionSession.ts  # 세션 관리 (확장된 useRealtimeSession)
│   └── useSceneManager.ts        # Scene 전환 관리
├── quiz/
│   ├── QuizHost.tsx              # 호스트 뷰
│   ├── QuizParticipant.tsx       # 참여자 뷰
│   ├── QuizLeaderboard.tsx       # 리더보드
│   └── types.ts
├── vote/
│   ├── VoteHost.tsx
│   ├── VoteParticipant.tsx
│   ├── VoteResults.tsx
│   └── types.ts
├── this-or-that/
│   ├── ThisOrThatHost.tsx
│   ├── ThisOrThatParticipant.tsx
│   └── types.ts
├── word-cloud/
│   ├── WordCloudHost.tsx
│   ├── WordCloudParticipant.tsx
│   ├── WordCloudDisplay.tsx
│   └── types.ts
├── personality/
│   ├── PersonalityHost.tsx
│   ├── PersonalityParticipant.tsx
│   └── types.ts
├── bingo/
│   ├── BingoHost.tsx
│   ├── BingoParticipant.tsx
│   ├── BingoBoard.tsx
│   └── types.ts
└── common/
    ├── ReactionBar.tsx           # 이모지 리액션
    ├── QAPanel.tsx               # Q&A 패널
    ├── ChatPanel.tsx             # 채팅 패널
    ├── ParticipantCounter.tsx    # 참여자 수
    └── EmojiRain.tsx             # 이모지 애니메이션
```

#### B. audience-engage 앱 (통합 플랫폼)

```
src/app/audience-engage/
├── page.tsx                      # 메인 (참여 코드 입력)
├── create/page.tsx               # 세션 생성 (슬라이드 + Scene 선택)
├── host/[code]/page.tsx          # 호스트 뷰 (SceneManager 사용)
├── join/[code]/page.tsx          # 참여자 뷰 (SceneManager 사용)
├── components/
│   ├── SceneManager.tsx          # Scene 전환 관리
│   ├── SlideManager.tsx          # 슬라이드 + Scene 타임라인
│   ├── SlideUploader.tsx         # PDF/이미지 업로드
│   ├── GoogleSlidesEmbed.tsx     # Google Slides iframe
│   └── ControlPanel.tsx          # 호스트 컨트롤
├── hooks/
│   ├── usePresentation.ts        # 프레젠테이션 상태
│   └── useSlideSync.ts           # 슬라이드 동기화
├── store/
│   └── audienceEngageStore.ts    # Zustand 스토어
└── types/
    └── index.ts
```

#### C. 독립 앱 라우트 (기존 유지 + 공유 컴포넌트 사용)

```
src/app/realtime-quiz/
├── page.tsx                      # → features/interactions/quiz 사용
├── create/page.tsx
├── host/[code]/page.tsx
└── join/[code]/page.tsx

src/app/this-or-that/
├── page.tsx                      # → features/interactions/this-or-that 사용
└── ...

src/app/word-cloud/
├── page.tsx                      # → features/interactions/word-cloud 사용
└── ...

src/app/personality-test/
├── page.tsx                      # → features/interactions/personality 사용
└── ...

src/app/human-bingo/
├── page.tsx                      # → features/interactions/bingo 사용
└── ...

src/app/live-voting/
├── page.tsx                      # → features/interactions/vote 사용
└── ...
```

### 4.2 데이터베이스 스키마

#### A. 기존 테이블 활용

```typescript
// src/types/database.ts에 추가
export type AppType =
  | ...
  | 'audience-engage';
```

#### B. 새 테이블

```sql
-- 프레젠테이션 파일
CREATE TABLE presentation_files (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id UUID NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
  source_type TEXT NOT NULL CHECK (source_type IN ('google-slides', 'pdf', 'images')),
  source_url TEXT,  -- Google Slides URL
  slide_count INTEGER DEFAULT 0,
  status TEXT DEFAULT 'processing' CHECK (status IN ('processing', 'ready', 'error')),
  created_at TIMESTAMPTZ DEFAULT now()
);

-- 슬라이드 이미지 (PDF/이미지 업로드용)
CREATE TABLE slide_images (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  presentation_id UUID NOT NULL REFERENCES presentation_files(id) ON DELETE CASCADE,
  position INTEGER NOT NULL,
  image_url TEXT NOT NULL,
  thumbnail_url TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(presentation_id, position)
);

-- 프레젠테이션 상태 (실시간 동기화)
CREATE TABLE presentation_states (
  session_id UUID PRIMARY KEY REFERENCES sessions(id) ON DELETE CASCADE,
  current_slide INTEGER DEFAULT 1,
  is_presenting BOOLEAN DEFAULT FALSE,
  chat_enabled BOOLEAN DEFAULT TRUE,
  reactions_enabled BOOLEAN DEFAULT TRUE,
  anonymous_allowed BOOLEAN DEFAULT TRUE,
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- 슬라이드 아이템 (슬라이드 + 인터랙션 순서)
CREATE TABLE slide_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id UUID NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
  position INTEGER NOT NULL,
  item_type TEXT NOT NULL CHECK (item_type IN ('slide', 'poll', 'quiz', 'this-or-that', 'word-cloud', 'embed')),
  slide_index INTEGER,  -- presentation_files의 슬라이드 인덱스
  poll_id UUID REFERENCES polls(id) ON DELETE SET NULL,
  linked_session_code TEXT,  -- 다른 앱 세션 코드
  embed_url TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(session_id, position)
);

-- Q&A 질문
CREATE TABLE questions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id UUID NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
  participant_id UUID REFERENCES session_participants(id) ON DELETE SET NULL,
  author_name TEXT NOT NULL,
  body TEXT NOT NULL,
  like_count INTEGER DEFAULT 0,
  is_highlighted BOOLEAN DEFAULT FALSE,
  is_answered BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- Q&A 좋아요
CREATE TABLE question_likes (
  question_id UUID NOT NULL REFERENCES questions(id) ON DELETE CASCADE,
  participant_id UUID NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now(),
  PRIMARY KEY (question_id, participant_id)
);

-- 채팅 메시지
CREATE TABLE chat_messages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id UUID NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
  participant_id UUID REFERENCES session_participants(id) ON DELETE SET NULL,
  author_name TEXT NOT NULL,
  body TEXT NOT NULL,
  slide_position INTEGER DEFAULT 1,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- 이모지 리액션 집계
CREATE TABLE reaction_counts (
  session_id UUID PRIMARY KEY REFERENCES sessions(id) ON DELETE CASCADE,
  thumbs_up INTEGER DEFAULT 0,
  heart INTEGER DEFAULT 0,
  laugh INTEGER DEFAULT 0,
  clap INTEGER DEFAULT 0,
  party INTEGER DEFAULT 0,
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- Realtime 활성화
ALTER PUBLICATION supabase_realtime ADD TABLE presentation_states;
ALTER PUBLICATION supabase_realtime ADD TABLE slide_items;
ALTER PUBLICATION supabase_realtime ADD TABLE questions;
ALTER PUBLICATION supabase_realtime ADD TABLE chat_messages;
ALTER PUBLICATION supabase_realtime ADD TABLE reaction_counts;
```

### 4.3 실시간 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    Supabase Realtime                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Channel: audience-engage:{sessionId}                        │
│                                                              │
│  Subscriptions:                                              │
│    ├── postgres_changes: presentation_states (슬라이드 동기화)│
│    ├── postgres_changes: questions (Q&A)                     │
│    ├── postgres_changes: chat_messages (채팅)                │
│    ├── postgres_changes: votes (투표)                        │
│    ├── broadcast: emoji-reaction (이모지 브로드캐스트)       │
│    └── presence: 참여자 수 추적                              │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. 화면 설계

### 5.1 호스트 뷰 (`/host/[code]`)

```
┌─────────────────────────────────────────────────────────────┐
│  [←] 세션: ABC123                    👥 12명   🔗 공유      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────┬────────────────────────┐    │
│  │                            │                         │    │
│  │      현재 슬라이드         │    컨트롤 패널          │    │
│  │      (프리뷰)              │                         │    │
│  │                            │  [◀ 이전] [다음 ▶]     │    │
│  │                            │                         │    │
│  │      3 / 15                │  ☑ 채팅 활성화          │    │
│  │                            │  ☑ 리액션 활성화        │    │
│  │                            │  ☑ 익명 허용            │    │
│  │                            │                         │    │
│  └────────────────────────────┴────────────────────────┘    │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  📊 슬라이드 타임라인                                │    │
│  │  [1][2][3][📊][4][5][❓][6][7][🎯][8]...              │    │
│  │       ↑ 현재                                         │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌──────────┬──────────┬──────────┬──────────┐              │
│  │ Q&A (5)  │ 채팅 (12)│ 투표     │ 리액션   │              │
│  ├──────────┴──────────┴──────────┴──────────┤              │
│  │                                            │              │
│  │  ❓ API 연동은 어떻게 하나요?     👍 3    │              │
│  │  ❓ 가격 정책이 궁금합니다       👍 2    │              │
│  │  ❓ 데모 신청 가능한가요?        👍 1    │              │
│  │                                            │              │
│  └────────────────────────────────────────────┘              │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 참여자 뷰 (`/join/[code]`)

```
┌─────────────────────────────────────────────────────────────┐
│  발표 제목                                   슬라이드 3/15  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                                                      │    │
│  │                                                      │    │
│  │              현재 슬라이드 표시                      │    │
│  │              (호스트와 동기화)                       │    │
│  │                                                      │    │
│  │                                                      │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  ☆ 퀴즈: 정답을 선택하세요! (활성화된 인터랙션)     │    │
│  │                                                      │    │
│  │  [ A. 옵션 1 ]   [ B. 옵션 2 ]                       │    │
│  │  [ C. 옵션 3 ]   [ D. 옵션 4 ]                       │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌───────┬───────┬───────┬───────┬───────┐                  │
│  │  👍   │  ❤️   │  😂   │  👏   │  🎉   │                  │
│  └───────┴───────┴───────┴───────┴───────┘                  │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ [💬 Q&A]  [💭 채팅]                                  │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │                                                      │    │
│  │  질문을 입력하세요...                    [제출]     │    │
│  │                                                      │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 기존 인프라 활용

### 6.1 재사용 컴포넌트

| 기존 | 활용 |
|------|------|
| `useRealtimeSession` | 세션 관리 |
| `SessionHostLayout` | 호스트 레이아웃 |
| `SessionJoinFlow` | 참여 플로우 |
| `ParticipantList` | 참여자 목록 |
| `generateSessionCode` | 코드 생성 |

### 6.2 기존 앱 연동

```typescript
// 다른 앱 세션을 iframe으로 임베드
<iframe
  src={`/realtime-quiz?code=${linkedCode}&embed=true`}
  className="w-full h-full"
/>
```

---

## 7. 구현 계획

### Phase 0: 공유 컴포넌트 추출 (리팩토링)
- [ ] `src/features/interactions/` 폴더 생성
- [ ] 기존 6개 앱에서 핵심 로직 추출
  - [ ] realtime-quiz → `features/interactions/quiz/`
  - [ ] live-voting → `features/interactions/vote/`
  - [ ] this-or-that → `features/interactions/this-or-that/`
  - [ ] word-cloud → `features/interactions/word-cloud/`
  - [ ] personality-test → `features/interactions/personality/`
  - [ ] human-bingo → `features/interactions/bingo/`
- [ ] 공통 타입 정의 (`InteractionScene`, `SceneConfig` 등)
- [ ] `useInteractionSession` 훅 생성 (확장된 useRealtimeSession)
- [ ] 기존 앱 라우트가 공유 컴포넌트 사용하도록 수정

### Phase 1: audience-engage 기본 구조
- [ ] 라우트 구조 생성 (`/audience-engage/*`)
- [ ] 데이터베이스 마이그레이션 (presentation_files, slide_items 등)
- [ ] 세션 생성/참여 플로우
- [ ] SceneManager 컴포넌트 구현
- [ ] Google Slides 임베드
- [ ] 슬라이드 동기화 (호스트 → 참여자)

### Phase 2: Scene 통합
- [ ] SceneManager에 6개 Scene 연결
- [ ] 슬라이드 타임라인 UI (슬라이드 + Scene 혼합)
- [ ] Scene 전환 로직
- [ ] 호스트 컨트롤 패널

### Phase 3: 공통 기능
- [ ] Q&A 기능 (공통 컴포넌트)
- [ ] 실시간 채팅 (공통 컴포넌트)
- [ ] 이모지 리액션 (공통 컴포넌트)
- [ ] 참여자 수 표시

### Phase 4: 파일 업로드
- [ ] PDF → 이미지 변환 (pdf.js)
- [ ] 이미지 직접 업로드
- [ ] Supabase Storage 연동
- [ ] 썸네일 생성

### Phase 5: 고급 기능
- [ ] PPT 변환 (Cloudinary API)
- [ ] 발표자 노트
- [ ] 분석/리포트
- [ ] 템플릿 저장/불러오기

---

## 8. 기술 스택

| 영역 | 기술 |
|------|------|
| 프레임워크 | Next.js 15 App Router |
| 스타일링 | Tailwind CSS + shadcn/ui |
| 상태관리 | Zustand |
| 실시간 | Supabase Realtime |
| 스토리지 | Supabase Storage |
| PDF 파싱 | pdf.js (클라이언트) |
| 애니메이션 | Framer Motion |
| 차트 | Recharts |

---

## 9. 참고 자료

- Claper 분석: `claudedocs/claper-analysis.md`
- Auden 분석: `claudedocs/auden-architecture-analysis.md`
- 기존 realtime 아키텍처: `src/lib/realtime/`
- 기존 database 타입: `src/types/database.ts`
